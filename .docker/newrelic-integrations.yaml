integrations:
  - name: nri-postgresql
    env:
      PG_HOST: ${PG_HOST}
      PG_PORT: 5432
      PG_USER: ${PG_USER}
      PG_PASSWORD: ${PG_PASSWORD}
      PG_DATABASE: ${PG_DATABASE}
    labels:
      env: development
      role: database
license_key: ${NRIA_LICENSE_KEY}
